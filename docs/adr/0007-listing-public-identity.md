# Listing public identity

Each Listing has an immutable, globally unique public slug in addition to its internal primary key. Public routes use the slug so Listing links remain stable when a Seller changes the title.
